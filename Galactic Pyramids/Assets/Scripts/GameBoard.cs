/*
using UnityEngine;

// Up and Down for the triangles like an arrow

public class Tile
{
    public bool Occupied { get; set; }
    public bool Captured { get; set; }

    public GameObject Pyramid;
}

public class GameBoard : MonoBehaviour
{    
    
    [SerializeField] public Tile[,] Tile = new Tile[11, 8];

    // Start is called before the first frame update
    void Start()
    {
        

        

       
    }
    // Update is called once per frame
    void Update()
    {
        
    }
     //           int i = ((y + 1) * x);
                    
   

      

  //              Instantiate(Tile[i], new Vector3(tileCordinates[y, x, 0], 0, tileCordinates[y, x, 1]), Quaternion.Euler(90,0,0));
  //              Tile[i].tag = $"Tile {s}";
    
}
*/